// if you selecct any tag in the inspect then
// $0 returns the most recently selected element or JavaScript object
// $1 returns the second most recently selected one, and so on.

let item = $0;
console.log(item);